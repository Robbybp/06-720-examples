/* Forward declarations for logging functions. Header file really not
 * necessary for functions this simple, but will be convenient if these
 * get more complicated.
 */

int print_header(void);

int log_iter(int iter, double eps);
